rootProject.name = "aoc2025"

pluginManagement {
    repositories {
        gradlePluginPortal() // Default repository for community plugins
        mavenCentral()       // For plugins hosted on Maven Central
        google()             // Required if using Android Gradle plugins
    }
}

